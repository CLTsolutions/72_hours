import './Nasa.css'

// Function name matches file name
const Nasa = () => {
    // return must have one parent element
    return (
        <div>
            Also a change
            72 hour project
        </div>
    )
}

// Makes it available for import
export default Nasa;